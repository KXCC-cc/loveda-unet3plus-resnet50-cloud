_base_: resnet50_24gb.yaml

# 消融 C：50% 概率尝试包含目标类别的 crop；失败后回退普通随机 crop。
run:
  name: cloud_resnet50_24gb_classaware
  output_dir: runs/cloud_resnet50_24gb_classaware

data:
  class_aware_crop:
    probability: 0.5
    classes: [1, 2, 3, 4]
    min_target_pixels: 512
    max_attempts: 8
