_base_: resnet50_24gb_multiscale.yaml

# 组合候选：只有单项消融有效后再运行。
run:
  name: cloud_resnet50_24gb_multiscale_classaware
  output_dir: runs/cloud_resnet50_24gb_multiscale_classaware

data:
  class_aware_crop:
    probability: 0.5
    classes: [1, 2, 3, 4]
    min_target_pixels: 512
    max_attempts: 8
